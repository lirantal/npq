---
'npq': patch
---

Normalize complete maintainer email domains with Node.js built-ins and limit DNS warnings to simple two-label public domains. Multipart and otherwise ambiguous failures are reported as not evaluated.
